<?xml version="1.0" encoding="utf-8"?>
<!-- res/values-night/themes.xml -->
<!-- Dark theme is already the default; this file just confirms the night override uses the same theme.
     All dark tokens are already baked into Base.Theme.FTPServer. -->
<resources>
    <style name="Theme.FTPServer" parent="Base.Theme.FTPServer" />
</resources>
